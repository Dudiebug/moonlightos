# Fail-closed template. moonlightos-firewall renders literal approved peer IPs.
table inet moonlightos {
  chain input {
    type filter hook input priority filter; policy accept;
    tcp dport 3240 iifname "tailscale0" jump usbip_tailnet
    tcp dport 3240 jump usbip_lan
  }
  chain usbip_lan {
    drop
  }
  chain usbip_tailnet {
    drop
  }
}
